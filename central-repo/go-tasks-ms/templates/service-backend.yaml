apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-tasks-ms.fullname" . }}
  labels:
    {{- include "go-tasks-ms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-tasks-ms.selectorLabels" . | nindent 4 }}
