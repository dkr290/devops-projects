apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-tasks-ms.fullname" . }}-test-connection"
  labels:
    {{- include "go-tasks-ms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-tasks-ms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
